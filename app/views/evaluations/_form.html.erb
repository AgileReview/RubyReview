
<div class="field">
Reviewee:<br />
<%= @reviewee_name %>
</div>
<div class="field">
Reviewer<br />
<%= @reviewer_name %>
</div>


<% @questions.each do |q| %>
    <%= form_for(@evaluation) do |f| %>
      <% if @evaluation.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@evaluation.errors.count, "error") %> prohibited this evaluation from being saved:</h2>

          <ul>
          <% @evaluation.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.label q.questionText %>&nbsp;<%= f.text_field :answer_id %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>
<% end %>
